# Demo-request capture (audit H10). Both are PUBLIC values (anon key is
# designed to be shipped to browsers; demo_requests is INSERT-only for anon).
# Without these the "Book a demo" form shows an error instead of silently
# dropping the lead.
NEXT_PUBLIC_SUPABASE_URL=https://vhgruadourflpxuzuxfn.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
